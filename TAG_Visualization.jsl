Names Default To Here( 1 );New Window( "TAG Visualization", 	V List Box( 					Outline Box( "Step 1: Duration Markers",			H List Box(				Lineup Box( N Col( 2 ),					Button Box( "Create Duration Markers",						Include(							"~/Documents/JMP/Scripts/Tag_Visualization/durationmarkerandcount.jsl"						)					),					Text Box( "Creates a column which assigns values of 1 or 2 based on user input for Dwell Duration (1: Below input. 2: Above input" ),				),			),		),				Outline Box( "Step 2: Bank to Cap Conversion",			H List Box(				Lineup Box( N Col( 4 ),					Button Box( "Generate Caps from Banks",						Include( "~/Documents/JMP/Scripts/Tag_Visualization/bank_to_cap.jsl" )					), 					Text Box( "Creates a Bank column which assigns values 1-4 for the cell. Then creates four cap columns and assigns a value of 1 if the cell in the bank corresponds to the cap collumn.") 				),			),		), 				Outline Box( "Step 3: Generate Wait Time",			H List Box(				Lineup Box( N Col( 4 ),					Button Box( "Calculate Wait Time",						Include( "~/Documents/JMP/Scripts/Tag_Visualization/wait_time.jsl" )					), 					Text Box( "Calculates wait time based off of the difference of the start time of the next event, and the stop time of the previous event.")				),			),		), 					Outline Box( "Step 4: Calculate Background Rate",			H List Box(				Lineup Box( N Col( 2 ),					Button Box( "Calculate Background Rate", 					Include( "~/Documents/JMP/Scripts/Tag_Visualization/tabulate_rate.jsl" ) ),				),			),		),		Outline Box( "Plots at a Glance",			H List Box(				Lineup Box( N Col( 2 ),					Button Box( "Generate Histograms",						Include(							"~/Documents/JMP/Scripts/Tag_Visualization/threecolumnshist.jsl"						)					),					Text Box( "Generates Dwell Start (S), Median Curent Level, and Dwell Duration (S) Histograms" ), 					Button Box( "Generate Cell vs Dwell Start Time vs Duration Marker",						Include(							"~/Documents/JMP/Scripts/Tag_Visualization/cell_vs_start_vs_durationmarker.jsl"						)					),					Text Box( "Assumes: Cell, Dwell Start (S), Duration Marker" ), 										Button Box( "Plot: Start Time - Scatter",						Include( "JMP/Scripts/ACVisualization/startscatter.jsl" )					),					Text Box( "Assumes: Dwell Start (S)" ), 										Button Box( "Plot: Level - Scatter",						Include( "JMP/Scripts/ACVisualization/levelscatter.jsl" )					),					Text Box( "Assumes: Median Curent Level" ),				),			),		), 				Outline Box( "Filters",			H List Box(				Lineup Box( N Col( 2 ),					Button Box( "Create Subsets for Flows",						Include(							"~/Documents/JMP/Scripts/Tag_Visualization/Pre_Post_Subset.jsl"						),					),				),			),		),			Outline Box( "Remove Bad JuJu",			H List Box(				Lineup Box( N Col( 2 ),					Button Box( "Tabulate & Histogram: Bad JuJu", 					Include( "~/Documents/JMP/Scripts/Tag_Visualization/RemoveBadActors.jsl" ) ),					Text Box( "Assumes: Cell" ),				),			),		),				Outline Box( "Final Report",			H List Box(				Lineup Box( N Col( 2 ),					Button Box( "Generate Background Level and Dwell Duration",						Include(							"~/Documents/JMP/Scripts/Tag_Visualization/distribution_level_duration_wait.jsl"						)					),					Text Box( "Background Level, Dwell Duration" ),	),),),),);